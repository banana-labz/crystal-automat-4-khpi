import styled from "styled-components"

export const AutomatContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-self: center;
    gap: 1px;

    > * {
        width: 100%;
    };
`
